<% content_for(:header_links) do %>
  <a class="list" href="/lists">All Lists</a>
<% end %>

<section id="todos">
  <header>
    <h2><%= @list[:name] %></h2>
    <ul>
      <li>
        <a class="edit" href="/lists/<%= @list[:id] %>/edit">Edit List</a>
      </li>
    </ul>
  </header>

  <!-- List todo items -->
  <ul>
    <% @list[:todos].each_with_index do |todo, index| %>
    <!-- If item is completed, <li> tag should have class="complete" -->
      <% if todo[:completed] %>
        <li class="complete">
      <% else %>
        <li>
      <% end %>
          <form action="/lists/<%= @list[:id] %>/todos/<%= index %>" method="post" class="check">
            <input type="hidden" name="completed" value="<%= !todo[:completed] %>" />
            <button type="submit">Complete</button>
          </form>
          <h3><%= todo[:name] %></h3>
          <form action="/lists/<%= @list[:id] %>/todos/<%= index %>/destroy" method="post" class="delete">
            <button type="submit">Delete</button>
          </form>
        </li>
    <% end %>
  </ul>
</section>

<!-- Enter a new todo item -->
<form action="/lists/<%= @list[:id] %>/todos" method="post">
  <dl>
    <dt>
      <label for="todo">Enter a new todo item:</label>
    </dt>
    <dd>
      <input name="todo" id="todo" placeholder="Something to do" type="text" value="<%= params[:todo] %>">
    </dd>
  </dl>
  <fieldset class="actions">
    <input type="submit" value="Add">
  </fieldset>
</form>

